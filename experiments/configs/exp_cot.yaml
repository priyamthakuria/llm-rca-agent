experiment_id: "E2_cot"
mode: "chain_of_thought"
model:
  backend: "openai"
  model_name: "gpt-4"
  temperature: 0.0
  max_tokens: 1024

prompt:
  template: "chain_of_thought"

data:
  test_set_path: "data/synthetic"
  test_split_ratio: 0.2

output:
  results_dir: "experiments/results"
  save_predictions: true

seed: 42

